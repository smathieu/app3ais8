function datatest    close all    clear all    clc    Fe = 2000;    f = 20;        falls = get_falls;    non_falls = get_non_falls;    colours = 'rgbcmykrgbcmykrgbcmyk';        % Plot falls%     data = falls;%     for i = 1 : length(data)/5%         figure%         hold on%         %         for j = 1:5%         plot_data = data((i-1)*5+j).Sensor4;%         plot_data = downsample_signal(plot_data, 100, Fe);%         plot_data = window_average(plot_data, 20);% %         plot_data = real(fft(plot_data, length(plot_data)));% %         s = length(plot_data)% %         plot_data = plot_data(floor(s/2) : s);% %         plot_data = diff(plot_data);%         plot(1:length(plot_data), plot_data, colours(j))%         end%     end        val = [];     for i = 1 : length(falls)        data = falls(i).Sensor4;        data = downsample_signal(data, 100, 2000);        val = [val, abs(abs(max(data)) - abs(min(data)))];    end    size_fall = mean(val)    min_fall = min(val)    max_fall = max(val)        val = [];    for i = 1 : length(non_falls)        data = non_falls(i).Sensor4;        data = downsample_signal(data, 100, 2000);        val = [val, abs(abs(max(data)) - abs(min(data)))];    end    size_non_fall = mean(val)    min_non_fall = min(val)    max_non_fall = max(val)